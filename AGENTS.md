# 数学建模项目规则

## 每次对话开始前的上下文阅读

除非用户明确要求跳过阅读，或明确将任务限定为与本项目内容无关的独立操作，每次涉及本项目的对话开始时，必须先阅读：

1. `problem/` 下的原题材料，至少包括 `problem/original_problem/` 中的原题及其配套材料。
2. `paper/sections/` 下当前已经写入的全部论文分节。

上述目录仅作上下文阅读，原题和已写论文不得因阅读而被修改。若目录结构、文件名称或阅读范围发生变化，应以当前仓库实际内容为准。用户对本次任务另有明确阅读范围或跳过要求时，以用户的最新明确要求为准。

## 建模手提供材料的优先级

若用户或建模手在当前 prompt、对话附件或项目材料中明确说明某项数据、参数、思路、公式、模型结构或假设来自建模手，则将其作为本项目的可信输入和工作依据，不要求先由 Codex 独立重算、反驳或改写。除非发现明显矛盾、单位或定义缺失，或用户要求复核，否则直接沿用并继续推进。

`model_cards/` 是可选的记录与复核材料，不是每次工作的前置门槛；不要求建模手为了让 Codex 开始工作而重复编写 model card。只有在相关模型卡确实包含本任务所需信息、建模手材料与现有实现冲突、关键公式或参数仍不清楚，或用户明确要求同步模型卡时，才查阅或更新对应文件。

1. 不得虚构数据、参数、实验结果或参考文献。
2. 原始数据只能读取，不得直接修改。
3. 所有中间数据保存到 data/processed。
4. 每个由代码计算得到的数值结论必须能追溯到具体代码和输出文件；建模手明确提供的原始数据、参数或假设可直接作为输入，并标明其来源。
5. 所有图片必须由代码生成，不得手工伪造。
6. 所有表格同时输出 CSV 和 LaTeX 版本。
7. 所有脚本使用相对路径，不得写死本机绝对路径。
8. 涉及随机过程时必须固定随机种子。
9. 每个模型实现后必须进行合理性检查。
10. 对建模手未明确给出、且会影响模型或结论的关键数学决策不得自行猜测，记录到 docs/open_questions.md；建模手已明确给出的决策不再重复列为待确认项。
11. 修改代码前按需查阅对应 model_cards 文件；若建模手已在 prompt 或项目材料中明确提供所需模型信息，且不存在冲突或歧义，不得因缺少 model card 而阻塞实现。
12. 不得为了让代码运行而私自改变模型公式。
13. 输出图片使用适合论文的尺寸与分辨率。
14. 中文图片必须避免乱码。
15. 写入论文的计算结果数值必须来自 outputs 目录；建模手明确提供的题设数据、输入参数和模型假设可直接引用，但应与计算结果区分。
