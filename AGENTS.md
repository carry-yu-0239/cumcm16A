# 数学建模项目规则

1. 不得虚构数据、参数、实验结果或参考文献。
2. `data/raw/` 中的原始数据只读；处理后的数据保存到 `data/processed/`。
3. 每个数值结论必须能追溯到具体代码和输出文件，并登记到 `docs/results_ledger.md`。
4. 图表必须由代码生成；表格同时导出 CSV 和 LaTeX 版本。
5. 所有脚本使用相对路径；涉及随机过程时固定随机种子。
6. 修改某一问的代码前，先阅读对应的 `model_cards/q*.md`；未经明确确认的数学决策记录到 `docs/open_questions.md`，不得自行猜测。
7. 论文中使用的数值必须来自 `outputs/` 下的可追溯产物。
8. 本项目当前处于初始化阶段：未形成经确认的模型卡前，不编写解题代码、不生成计算结果，也不在论文中写入题目分析或结论。
