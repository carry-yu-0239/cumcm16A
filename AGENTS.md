# 数学建模项目规则

1. 不得虚构数据、参数、实验结果或参考文献。
2. 原始数据只能读取，不得直接修改。
3. 所有中间数据保存到 data/processed。
4. 每个数值结论必须能追溯到具体代码和输出文件。
5. 所有图片必须由代码生成，不得手工伪造。
6. 所有表格同时输出 CSV 和 LaTeX 版本。
7. 所有脚本使用相对路径，不得写死本机绝对路径。
8. 涉及随机过程时必须固定随机种子。
9. 每个模型实现后必须进行合理性检查。
10. 未明确的数学决策不得自行猜测，记录到 docs/open_questions.md。
11. 修改代码前先阅读对应 model_cards 文件。
12. 不得为了让代码运行而私自改变模型公式。
13. 输出图片使用适合论文的尺寸与分辨率。
14. 中文图片必须避免乱码。
15. 写入论文的所有数值必须来自 outputs 目录。